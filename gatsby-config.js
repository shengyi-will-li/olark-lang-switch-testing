/**
 * @type {import('gatsby').GatsbyConfig}
 */
module.exports = {
  siteMetadata: {
    title: `Olark-Playground`,
    siteUrl: `https://www.yourdomain.tld`,
  },
  plugins: [],
};
require("dotenv").config();
